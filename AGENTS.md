# Codex Relay Agent Guide

## 项目定位

Codex Relay 是一个本机优先的 Tauri 2 桌面应用，用于管理 Codex 档案、聚合本机任务，并向协作渠道发送任务状态通知。

当前仓库仍处于业务实现阶段。不要把 PRD 中的目标能力当作已经存在，也不要为了补齐功能而改变未请求的产品范围。

## 先读什么

1. 先查看 `git status --short` 与相关 diff，保留用户已有的未提交修改。
2. 阅读 `README.md` 了解运行方式、目录与项目基础。
3. 阅读 `docs/PRD.md` 了解产品范围、验收与发布门槛。
4. 阅读 `CONTRIBUTING.md` 确认质量门禁。
5. 再按变更范围阅读相应代码与局部说明：
   - `src/features/README.md`：业务功能的模块边界。
   - `src/shared/README.md`：跨功能稳定复用的边界。
   - `src-tauri/capabilities/`、`src-tauri/tauri.conf.json`：桌面端配置。

`docs/requirements.md` 已由 `docs/PRD.md` 替代且正由用户移除；不要引用、恢复或同步它。

## 代码与职责边界

- `src/app/`：应用入口、页面壳与全局装配。
- `src/features/`：按业务领域组织组件、状态、测试和对外导出；不得让功能直接依赖另一个功能的内部实现。
- `src/shared/`：仅放多个功能长期稳定复用的 UI、工具和类型；业务专属代码留在对应 `features` 目录。
- `src/styles/`：全局样式和设计基础。
- `src-tauri/`：Rust 系统能力、Codex 进程生命周期、网络和 Tauri command；前端通过它调用本机能力。

保持 TypeScript 严格类型检查。新增 Rust command 时，先定义输入/输出类型，注册 command manifest，并为成功与失败路径编写测试。

## 工作方式

1. 将每次改动限制为一个明确、可验证的目标；先关联 `docs/PRD.md` 的相关需求与验收项，再改代码。
2. 先复现或检查当前行为，再做最小实现；不要重构无关区域或覆盖用户修改。
3. 在系统、网络或协议边界处使用显式类型、错误路径和可测试的降级行为，不依赖猜测的数据形状。
4. 功能完成时同时更新必要的测试与文档；记录实际运行的验证命令和结果。没有验证证据时，不要声称完成。
5. 若重复出现规则遗漏、评审反馈或导航成本，再更新本文件或添加贴近目录的说明；根文件保持为简短导航，不复制 PRD 全文。

## 验证矩阵

按改动范围运行最小相关检查：

| 改动                                    | 必跑命令                                               |
| --------------------------------------- | ------------------------------------------------------ |
| 前端 TypeScript/React                   | `pnpm test`、`pnpm lint`、`pnpm typecheck`             |
| Rust                                    | `pnpm rust:format`、`pnpm rust:lint`、`pnpm rust:test` |
| Tauri 配置、capability、CSP、插件或打包 | `pnpm tauri:build`                                     |
| 交付前的完整改动                        | `pnpm check` 与 `git diff --check`                     |

不要运行会改写文件的格式化命令，除非该格式化本身是当前任务的一部分。若某项检查无法运行，说明原因、已运行的替代检查及剩余风险。

## 评审清单

- 新系统能力、文件访问、Shell、插件或远程来源是否与当前功能直接相关？
- 档案、运行目录、任务与通知的行为是否符合需求？
- 是否只依赖可验证的数据形状，并为 CLI 缺失、版本不兼容和登录失效提供可测试的结果？
- 是否有针对失败与跨档案行为的测试，而非只验证成功路径？

使用 Conventional Commits。提交前通过适用检查；修改 Tauri 配置或打包逻辑时，在提交或 PR 说明中记录影响范围。
